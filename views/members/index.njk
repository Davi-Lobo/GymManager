{% extends "layout.njk" %}

{%block content%}
    <div class="_grid">

        <div class="section-container">
            <a href="members/create">Novo membro</a>

            <table class="table-container">
                <thead>
                    <tr class="titles">
                        <th>Instrutor</th>
                        <th>Atendimento</th>
                        <th>Ação</th>
                    </tr>
                </thead>
                <tbody>
                    {% for member in members %}
                        <tr>
                            <td class="user-cell">
                                <img class="avatar" src="{{member.avatar_url}}" alt="Instructor Avatar" width="80" height="80"/>
                                <span class="name">{{member.name}}</span>
                            </td>
                            <td>{{member.skills}}</td>
                            <td>
                                <a class="button" href="/members/{{member.id}}"><span>Ver</span></a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{%endblock content%}